# ToxGraph Dependencies
# Install PyTorch first (see README for CUDA-specific instructions):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# Then install the rest:
#   pip install -r requirements.txt

torch>=2.0.0
torch_geometric>=2.4.0
rdkit
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
streamlit>=1.28.0
